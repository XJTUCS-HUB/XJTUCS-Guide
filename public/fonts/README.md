# Anthropic Serif Web

Place the licensed webfont files in this directory using these names:

- `anthropic-serif-regular.woff2`
- `anthropic-serif-medium.woff2`

The site deliberately does not download or hotlink proprietary font files. The CSS falls back to a serif stack until licensed files are supplied.
